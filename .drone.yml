compose:
  database:
    image: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=testing

  commands:
    - sh build_script.sh

  notify:
    email:
      recipients:
        - caleb.carvalho@gmail.com