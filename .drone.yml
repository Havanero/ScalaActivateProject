cache:
  mount:
    - .git
    - .ivy2

build:
  image: hseeberger/scala-sbt:latest

  commands:
    - mkdir -p $HOME/.ivy2
    - echo hello world
    - sbt compile
    - sbt docker:publishLocal
    - ls

cache:
  mount:
    - .git
    - .ivy2
